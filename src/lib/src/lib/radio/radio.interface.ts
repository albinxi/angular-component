// Interface of radio emit event
export interface IRadioEmit
{
  length?: number;      // total length
  active?: number;      // activated index
  selected?: boolean;   // current click radio status
}
